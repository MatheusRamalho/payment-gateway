import { TicketPayInfo } from './TicketPayInfo'
import { TicketDivider } from './TicketDivider'
import { TicketInfo } from './TicketInfo'
import { TicketRoot } from './TicketRoot'

export const Ticket = {
    Root: TicketRoot,
    Info: TicketInfo,
    PayInfo: TicketPayInfo,
    Divider: TicketDivider,
}
